apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: external-secrets
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: external-dns

# Self-managed clusters do not have EKS IRSA automatically. Attach AWS access
# through a separately configured OIDC provider or a dedicated node role before
# deploying controllers that call AWS APIs.
